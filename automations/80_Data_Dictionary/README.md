# 80 Data Dictionary

Normalizes `Data_Dictionary`, fills `write_policy`, and cleans the second Key man header.

Column policy: the dictionary table is found by headers before rewriting, so it can move around.
